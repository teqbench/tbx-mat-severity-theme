# Thin caller — delegates to the org-wide reusable Dep Compat Check workflow.
# See: https://github.com/teqbench/.github/.github/workflows/dep-compat-check.yml
#
# Tracking epic: https://github.com/teqbench/tbx-mat-severity-theme/issues/46

name: Dependency Compatibility Check

on:
  schedule:
    - cron: "0 12 * * *"
  workflow_dispatch:

jobs:
  check:
    uses: teqbench/.github/.github/workflows/dep-compat-check.yml@v4.1.3
    with:
      epic-issue-number: 46
    secrets: inherit
